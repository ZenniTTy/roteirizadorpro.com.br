/// Cor de marcação da parada — exatamente 5 valores (fato F10, dump v3.65.1).
/// O mapeamento para tokens visuais do protótipo vive na camada de UI,
/// não aqui (domínio puro).
enum StopColor { blue, teal, purple, pink, orange }
