import 'package:flutter/material.dart';

class AddStopPage extends StatelessWidget {
  const AddStopPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('AddStop — Task 15')));
}
