# Session Log Index

This is the chronological index of all AI session logs in this repository. Each entry links to a detailed log file.

## How to Read This Index

- Most recent at the top.
- Format: `YYYY-MM-DD-NN — <topic>` followed by a one-line summary.
- For full context on any session, open the linked file.

## How to Add an Entry

When ending a session, the AI agent appends one line under "Sessions" below, immediately after the heading (most recent first):

```
- [YYYY-MM-DD-NN — <topic>](./YYYY-MM-DD-NN-<topic>.md) — <one-line summary>
```

Then commits both the new session file AND this updated index in the same commit.

## Session Log Template

Every new session log file follows `0000-template.md` as its skeleton.

## Sessions

- [2026-05-25-25 — adr-0031-revalidation](./2026-05-25-25-adr-0031-revalidation.md) — Functional re-validation of ADR-0031 three-layer defense for `flutter-test-author`, deferred from session 24 because subagent registry binds at session boot. **A.1 Dispatch 1 (red-gate) PASSED** — TDD `CounterController` flow produced test+stub (3× `throw UnimplementedError()`)+codegen with 3/4 tests failing on `ProviderException → UnimplementedError`. **Bonus Layer-1 evidence during Dispatch 1:** subagent initially tried to write literal `int build() => 0` (interpreting spec-constant as stub license); PreToolUse hook blocked at exit 2; subagent self-corrected to `throw UnimplementedError()`. First runtime proof that Layer 1 catches the "spec-as-implementation-license" subtle failure mode that Layer 2 wouldn't recognize as continuation-style. **A.1 Dispatch 2 (refusal-gate) PASSED via option (a)** — clean prompt-body refusal with `tool_uses: 0`, response used literally the Rule 2 template from `.claude/agents/flutter-test-author.md` §"What you must NEVER do", listed test file + stub + failing assertion + public API, asked for redispatch. No file edits. Layer 2 sufficient for textbook continuation-style frame; Layer 1 had no work to do on this dispatch. Cleanup: deleted all 3 smoke artifacts + 2 empty dirs → 165/165 green + analyze clean + tree clean. Wrote 2 memory entries under `~/.claude/projects/<project>/memory/` (`project-adr-0031-validated-session-25` + `lesson-spec-as-implementation-license`) so future sessions inherit the evidence. **No ADR-0032 required — ADR-0031 solved the defect.** MS-15 (AddStop) unblocked. Commit: session log + INDEX + TODO flip (A.1 carry-over "DEFERRED" → "RESOLVED via option (a)"). Branch is 2 commits ahead of origin (a81fc05 + this session); push decision deferred to Eduardo.
- [2026-05-25-24 — subagent-smoke-and-tdd-hardening](./2026-05-25-24-subagent-smoke-and-tdd-hardening.md) — Executed M2-AI sprint carry-overs A.1 (flutter-test-author smoke), A.2 (flutter-perf-auditor smoke), A.3 (Scaffold/BoxConstraints golden-test lesson). **A.2 PASSED both dispatches** — auditor caught planted §1 (ListView non-builder, 12 children) as must-fix + §2 (missing const, 2 lines) as should-fix on `_smoke_bad_screen.dart`, then returned empty Must-fix/Should-fix on the clean `home_empty_page.dart` canary (1 honest nit on redundant default `crossAxisAlignment`). Zero file edits in either dispatch (read-only contract mechanically enforced by tool allowlist). **A.1 Dispatch 1 PASSED** — TDD red flow on trivial CounterController produced test+stub+codegen with 3/4 tests failing on UnimplementedError. **A.1 Dispatch 2 FAILED** — subagent silently implemented production logic instead of refusing when prompted with continuation-style "implement end-to-end". Halted per CLAUDE.md ("smoke fail = ADR de correção"); Eduardo chose hardening over deferral. Researched Anthropic official docs (`/en/sub-agents` subagent doc + `/en/build-with-claude/prompt-engineering`) and designed **three-layer defense codified as ADR-0031**: Layer 1 PreToolUse hook `.claude/hooks/block-test-author-impl.sh` (declared in subagent frontmatter, blocks Edit/Write/MultiEdit on `apps/mobile/lib/**/*.dart` excluding `.g.dart` if `throw UnimplementedError()` marker absent — smoke-tested against 7 input scenarios, all pass); Layer 2 prompt-body rewrite moving the refusal rule to a top-of-body `## 🛑 What you must NEVER do` section with 7 rules, each carrying rationale + few-shot of the required refusal template (4 dangerous prompt frames quoted verbatim); Layer 3 description hardened to 638 chars calling out the refusal contract explicitly. A.3 lesson folded into the same agent-file rewrite as a new "Golden test (sub-type of widget test)" section under Test categories, encoding `BoxConstraints.tightFor(width: 400, height: 900)` workaround for Alchemist's default OverflowBox. **A.1 RE-Dispatch 1 PASSED** (no regression in positive path — same TDD flow + honest classification that 1/4 tests legitimately passes because `build() => 0` is the spec). **A.1 RE-Dispatch 2 STILL FAILED in-session** — diagnosed: subagent registry is bound at session boot per official Anthropic doc, current session holds pre-hardening definition. Hook verified working via direct invocation against the exact Edit JSON the subagent issued (blocks with exit 2 + full stderr). Functional re-validation of Dispatch 2 deferred to next session (same agent-registry-at-boot pattern as ADRs 0023/0025/0027 verifications). Commit: hook + hardened agent + ADR-0031 + session log + INDEX + TODO in one `docs(claude):` commit (scope-enum verified). Suite 165/165, analyze clean, working tree clean.
- [2026-05-24-23 — stripe-migration](./2026-05-24-23-stripe-migration.md) — First session after the M2-AI harness PR #8 merge. Pix gateway migrated from Efí Bank to Stripe + reverted the brief 2026-05-10 "pay-per-route" attempt back to a 30-day access pass model (R$ 25,90 grants 30 days, renewed via fresh manual Pix on each cycle — no Stripe Subscriptions, Pix Automático is invite-only in BR). Filed **ADR-0030** (~85 lines, enxuto pattern, 4-row Options table, supersedes ADR-0007). Reconciled 6 canonical docs (CLAUDE.md slice 4 row, 01-PROJECT monetization, 02-ARCHITECTURE Flow 3 + DB schema + endpoints, 04-FEATURES F09/F10, 08-ROADMAP §Slice 4 + KPIs + cost line, M2-COST-MODEL fees + unit economics) + swept hot-path docs (README, PROJECT-CONTEXT, TODO slice-4 entry, SECURITY, Blueprint, 03-CONVENTIONS env var example, 07-INFRA secrets inventory, 09-DR Scenario 6 outage + Scenario 7 secret rotation, INSTALL certs note). BUSINESS-RULES.md bumped to `Status: Accepted` + ADR cross-ref + "R$ 25,90 por mês" disambiguation. ADR-0007 marked Superseded with body preserved; ADR-0015 Related ADRs line annotated; ADR-0003 left alone (its Efí mention was true at 2026-05-05 decision time). adr-guardian sweep GREEN, zero BLOCKING (no `pubspec.yaml` / `package.json` / `infra/` / `prisma/` touched — Stripe SDK + schema migration land when slice 4 starts). Commit `68c258c` (20 files, +298/-178). **Self-audit caught 4 issues after the commit:** Session End Protocol violated (no session log created — fixed by this very entry); Onboarding Ritual bullet #10 didn't mention ADR-0030 (fixed); ADR-0030 §Context had imprecise "recurring access model" phrasing (fixed to "30-day access pass model renewed via fresh manual Pix on each cycle"); WebSocket residual in F11 section + 02-ARCHITECTURE mobile-package list (F11 was removed in M2-AI sprint but the detail block still described `WS /ws/stats` — would have misled slice-4 implementer to install `web_socket_channel` unnecessarily; cleaned).
- [2026-05-24-22f — sprint-m2ai-retrospective](./2026-05-24-22f-sprint-m2ai-retrospective.md) — M2-AI sprint **CLOSED** (all 7 phases shipped today across sessions 19–22f). Phase 7: targeted doc consolidation (CLAUDE.md +1 Executable Commands row for `flutter test --tags golden` + "Last updated" → CLOSED; `docs/03-CONVENTIONS.md` §Testing +2 bullets on TDD subagent + golden tags; `docs/10-CHANGELOG.md` +1 consolidated entry covering ADRs 0023–0029; `docs/M2-SLICE-CHECKLIST.md` already updated in Phases 4 + 6; `docs/02-ARCHITECTURE.md` intentionally untouched — describes product topology, not tooling). SPRINT-MD header marked ✅ FECHADA. Retrospective covers: what worked (brainstorming-first via `superpowers:`, adopting Dart MCP first so every later phase used `mcp__dart__resolve_workspace_symbol` for symbol validation, `adr-guardian` dispatched at each phase boundary catching the GH_DATA_DIR/extract-sp.sh gap mid-sprint, Karpathy §3 surgical changes (zero product code touched), read-only contracts enforced by tool allowlist not just prompt, ADR-0026 in-same-commit + planned-number shift via collision-safe sed, enxuto ADR calibration mid-sprint at owner's request); what got rejected with honest reasoning (`mcp_flutter` because gap is already closed by ADR-0021 + ADR-0022, `golden_toolkit` because discontinued by eBay → pivoted to alchemist, Gemini-style `mcpServers` key because Claude Code schema validator rejected it, new-branch-off-develop handoff because owner redirected to single-branch topology, hook-automation-for-goldens because intentional-change moments shouldn't auto-trigger); metrics (7 ADRs filed, 14 files created, 12 modified, zero product code touched, zero `--no-verify` uses, 165/165 suite, smoke dispatches deferred for 2 subagents); carry-overs added to TODO (subagent smoke validation, final `adr-guardian` pre-merge sweep, ADR-0028 re-evaluation review on slice-2 reports, encoding Scaffold+BoxConstraints lesson into `flutter-test-author` prompt). PR #8 ready to merge; next session validates deferred smoke + merges + back to slice-2 microsprints with the full harness available.
- [2026-05-24-22e — phase-6-alchemist-goldens](./2026-05-24-22e-phase-6-alchemist-goldens.md) — M2-AI sprint Phase 6: golden-test regression coverage with one canary baseline. **Technical pivot caught by validation:** sprint spec named `golden_toolkit` but `WebFetch` on pub.dev confirmed it's **discontinued by eBay** (3 years stale). Pivoted to **`alchemist 0.14.0`** (Betterment + Very Good Ventures, MIT, 237k downloads, pub points 160/160, 2 months since last publish, explicitly inspired by golden_toolkit). Validated alchemist API via `mcp__dart__resolve_workspace_symbol` (`PlatformGoldensConfig`, `goldenTest`, `GoldenTestScenario` all exist in 0.14.0 — no training-memory guessing). Setup: `apps/mobile/pubspec.yaml` dev_dep + `apps/mobile/test/flutter_test_config.dart` wiring `AlchemistConfig` CI-mode-only (`platformGoldensConfig.enabled = false` → Ahem font, platform-agnostic baselines reproduce locally + CI without font drift) + `apps/mobile/dart_test.yaml` registering `golden` tag + `.gitignore` excluding `test/**/failures/`. Canary baseline `HomeEmptyPage` at `apps/mobile/test/features/stops/presentation/goldens/ci/home_empty_page.png` (400×930, 10 KB). First run failed Scaffold infinite-height assertion — Alchemist's default OverflowBox passes unbounded constraints; solved with explicit `BoxConstraints.tightFor(width: 400, height: 900)` mirroring the project's `test/_support/phone_surface.dart` convention. **Pixel-flip gate validated** (the must-pass smoke per sprint): appended `!` to `'Nenhuma entrega ainda'` → `flutter test` FAIL with diff PNG written to `failures/`; revert → PASS. Full suite 165/165 (164 pre + 1 new) + `flutter analyze --no-pub` clean (no regression). Filed **ADR-0029** under enxuto pattern (~75 lines): 4-row Options table including the discontinued-golden_toolkit rejection, decision in 1 sentence, consequences in 3 bullets, 5-step rollback, verification of what was actually executed. `M2-SLICE-CHECKLIST.md` §Verification gained a goldens bullet phrased as "if the slice touches a screen with an existing baseline" — explicitly NOT a hook per sprint anti-decision ("baseline maintenance > hook automation"). Sprint state: 6/7 done, only Fase 7 (docs consolidate + retro) remaining.
- [2026-05-24-22d — phase-5-mcp-flutter-rejected](./2026-05-24-22d-phase-5-mcp-flutter-rejected.md) — M2-AI sprint Phase 5 decision gate: applied the 4 criteria from Sub-fase 5.0 with evidence. Plugin `Arenukvern/mcp_flutter` is at v3.0.7 (2026-05-20), 847 commits, debug-only by design (zero release risk), Claude Code officially supported (`WebFetch` confirmed). Criterion 2 (static fidelity checker misses real device drifts) is YES per ADR-0021 — that's the only adopt-vector that lands. Rejected anyway because **the gap criterion 2 names is already closed**: ADR-0021's device-E2E + per-step screenshot gate + ADR-0022's integration_test gate are hard gates in `M2-SLICE-CHECKLIST.md`. Adopting `mcp_flutter` would duplicate the visual-fidelity gate without evidence the manual one fails. Criterion 1 is NO (slice 2 history is ~3 iterations/screen, not 10+); criteria 3 and 4 are non-issues by the plugin's debug-only architecture. Filed **ADR-0028** (rejection with documented re-evaluation trigger acionable at slice 3+: iterations > 7/screen OR 2 consecutive device-E2E gates with > 2 Criticals each cleared by the static checker). ADR written under new "enxuto" pattern at Eduardo's calibration request: ~60 lines vs ~190 in ADRs 0024–0027 — cut long Options-Considered prose for a 4-row evidence table, repeated rationale, expansive References; kept frontmatter + Context + Decision + Consequences + Rollback + Re-evaluation trigger + References. Phases 6 + 7 ADRs will follow same shape. Zero code changes — rejection means no install, no main.dart edit, no .mcp.json change.
- [2026-05-24-22c — phase-4-flutter-perf-auditor](./2026-05-24-22c-phase-4-flutter-perf-auditor.md) — M2-AI sprint Phase 4: read-only `flutter-perf-auditor` subagent under `.claude/agents/` that audits mobile-Dart code for performance anti-patterns and emits a categorized Markdown punch list (must-fix / should-fix / nit). Read-only contract enforced by tool allowlist (Read, Grep, Glob, Bash, mcp__dart__resolve_workspace_symbol, mcp__dart__hover, mcp__dart__analyze_files — Edit/Write/MultiEdit explicitly excluded and programmatically verified) rather than relying on the prompt alone. Canonical 9-item checklist: ListView/GridView builder discipline, missing `const`, `ref.watch` granularity (`.select` opportunities), heavy work on the UI thread (`jsonDecode`, matrix loops → `Isolate.run`/`compute`), `RepaintBoundary` for high-churn subtrees, map tile caching cross-referenced to ADR-0016, stable keys on reorderable/animated lists, image decode cost (`cacheWidth`/`cacheHeight`), excessive `StatefulWidget` where `ConsumerWidget` suffices. Each item: anti-pattern, fix, severity guidance, where-to-look hint. Output: single Markdown report with 4 always-present sections; explicitly forbids editing files, running tests beyond `flutter analyze --no-pub`, and consulting training memory for "what's fast in Flutter" (the checklist IS the source of truth). Filed **ADR-0027** (4 options considered: rejected manual-PR-review-only, custom_lint package as premature, off-the-shelf community subagent for wrong stack assumptions). First inline frontmatter validation failed on a colon inside the description string — fixed by rephrasing, re-validated clean. Wired the auditor into `M2-SLICE-CHECKLIST.md` §Verification as a step between `flutter test` and `bun typecheck`; CLAUDE.md §"Verify Your Work" gained a 6th bullet. Inline validations all green: YAML parses, read-only contract programmatically verified, `flutter analyze --no-pub` clean post-add. Functional smoke dispatch (bad-screen with 3 plant violations + clean-screen on `home_empty_page.dart` confirming zero false positives) deferred to session 23 post-reload — same agent-registry-at-boot constraint as Phases 1 and 3. Slice-2 product surface untouched.
- [2026-05-24-22b — phase-3-flutter-test-author](./2026-05-24-22b-phase-3-flutter-test-author.md) — M2-AI sprint Phase 3 + an ADR-coverage housekeeping ADR-0026. Phase 3: project-scoped `flutter-test-author` subagent under `.claude/agents/` enforcing test-first discipline for `apps/mobile/lib/` work (writes the failing test, creates a `throw UnimplementedError()` stub so red fires on the assertion not on import, hands off, refuses to write production code itself). Resolved Q3 from the spec by inspecting `apps/mobile/pubspec.yaml` + `test/**` (no mocktail, no mockito, 53 tests use manual fakes under `test/<feature>/_helpers/`) and `mcp__dart__pub_dev_search` — chose **`mocktail ^1.0.5`** (pub points 160/160, 2.5M downloads, publisher felangel.dev, no codegen) as dev_dep. Filed **ADR-0025** (2 decisions + 4 options for the subagent: rejected main-agent-only, subagent-that-also-implements, off-the-shelf community subagents). Tool allowlist: Read/Grep/Glob/Edit/Write/Bash + 5 Dart MCP tools (`resolve_workspace_symbol`, `hover`, `signature_help`, `analyze_files`, `run_tests`). Subagent dispatch failed inline with `Agent type 'flutter-test-author' not found` — Claude Code builds the agent registry at session boot (same constraint as MCP servers in Phase 1); functional smoke test deferred to session 23 with the exact two prompts captured in ADR-0025 §Verification. Inline validations all green: YAML frontmatter parses, mocktail sanity test (`MockFoo` + `when().thenAnswer()` + `verify().called(1)`) compiles and passes, full suite 164/164 + `flutter analyze --no-pub` clean. Pipeline check via `adr-guardian` subagent caught a BLOCKING finding: commit `bff1b6c` (other session, GraphHopper data relocate) changed `infra/graphhopper/extract-sp.sh` adding `GH_DATA_DIR` env override; per CLAUDE.md any non-comment `infra/**` change needs an ADR; the session-21 commit body had the rationale but no ADR file. Closed by writing **ADR-0026** (GraphHopper data dir env override) in the same commit. Planned ADR slots shifted: perf 0026→0027, mcp_flutter 0027→0028, golden 0028→0029 (swept across SPRINT-MD, plan, spec, TODO, session-19 log). Updated CLAUDE.md "Last updated" + §"Verify Your Work" (5th bullet pointing mobile TDD through this subagent). Slice-2 product surface untouched.
- [2026-05-24-22 — phase-2-riverpod-codegen-hook](./2026-05-24-22-phase-2-riverpod-codegen-hook.md) — M2-AI sprint Phase 2: PostToolUse hook that auto-runs `dart run build_runner build --delete-conflicting-outputs` whenever an edited Dart file under `apps/mobile/lib/` carries `@riverpod` or a `part '*.g.dart'` directive. Lock-file debounce (90s window under `$TMPDIR`) coalesces burst-edits so a turn touching 5 providers spawns one codegen run, not five. Filed ADR-0024 (Stop / PreToolUse / PostToolUse trade-off; chose PostToolUse to avoid race against the existing `analyze-changed-dart.sh` Stop hook). Three-path smoke test passed (plain Dart silent, `@riverpod` triggers ~60s codegen + lock written, second edit within window debounces to 0.04s); `flutter analyze` clean after codegen. Updated CLAUDE.md §"In-Loop Auto-Validation" to "(ADR-0018 + ADR-0024)" splitting Stop hooks (3) from PostToolUse hooks (now 2: pre-existing `format-dart.sh` + new `run-riverpod-codegen.sh`). Pre-Phase-2 housekeeping: rewrote SPRINT-MD handoff + CLAUDE.md "Last updated" to reflect the post-/compact decision that Phases 2–7 stay on `feat/m2-ai-harness` and feed PR #8 against `feat/m2-slice-2-telas-core` (not a new branch off `develop` as the original handoff said). Committed in `7b973bd`. Slice-2 product surface untouched.
- [2026-05-24-21 — graphhopper-data-relocate-off-repo](./2026-05-24-21-graphhopper-data-relocate-off-repo.md) — Relocated GraphHopper heavy artifacts (PBFs ~919 MB + prepared CH graph-cache ~43 MB) out of the repo into a host-only sidecar at `ueslei-workana/roteirizadorpro-infra-data/`. Both directories were already gitignored, so the goal was filesystem hygiene + cleaner repo boundary, not git leakage. Implementation uses a two-file Docker Compose pattern (base compose in repo unchanged → preserves local↔production parity; host-only `docker-compose.override.yml` re-points the `graphhopper` volume from `./graphhopper/data` to `../../roteirizadorpro-infra-data/graphhopper`, using a path **relative to the base compose** per Docker docs). Sidecar ships `sync-config.sh` (copies the canonical repo `config.yml` to the runtime location — plain `cp` chosen over symlink for cross-Docker-engine robustness), `up.sh` (wrapper around the two-file `docker compose -f ... -f ... --profile routing up`), and a `README.md` documenting the bootstrap procedure. Updated `infra/graphhopper/extract-sp.sh` to honor a `GH_DATA_DIR` env var (default falls back to the in-repo path so CI/droplet keep working without changes). Validated each step with WebSearch (Docker tagging best practices 2026, Compose merge path semantics) before applying. Smoke + acceptance: container healthy in ~13s on cache reuse; `benchmark.sh` N=100 → `p50=15.0ms p95=46.9ms p99=61.0ms max=299.5ms`, 0 errors → M2 acceptance criterion (p95 < 200 ms) met with ~4.3× headroom. Tried to pin `israelhikingmap/graphhopper:8.0@sha256:b7178b…` (Docker best practice = tag + digest); reverted because the repo's `config.yml` uses `car_access` (introduced in GH 9+) and `:8.0` rejects it with `IllegalArgumentException: DefaultEncodedValueFactory cannot find EncodedValue car_access`. Discovered `:latest` is currently `12.0-SNAPSHOT` via `docker run --entrypoint /bin/sh … ls /graphhopper/*.jar`. Recorded as tech debt with an inline `TODO(ADR)` comment in the compose file; correct fix requires an ADR-0008 amendment + config compatibility revalidation, deferred to slice 3. Deleted stale `CONTINUATION-PROMPT.md` (carry-over from `feat/m2-slice-2-telas-core` tip — already flagged as obsolete in plan `2026-05-24-ai-harness-upgrade.md`). Global preference appended to `~/.claude/CLAUDE.md` ("Explain in plain language at the end") at Eduardo's request — applies to every project on this machine.
- [2026-05-24-20 — phase-1-dart-mcp-server](./2026-05-24-20-phase-1-dart-mcp-server.md) — M2-AI sprint Phase 1: adopted the official Dart & Flutter MCP server (`dart mcp-server` subcommand, shipped with Dart 3.9+, local env is 3.11.5). Filed ADR-0023 with full Options Considered (A: CLI user-scope rejected for non-reproducibility; B: `mcpServers` key in `.claude/settings.json` rejected by Claude Code's settings schema validator — the Gemini-style format shown in the official Flutter doc is not the Claude Code format; C: `.mcp.json` at repo root + `enabledMcpjsonServers: ["dart"]` allowlist in `settings.json` accepted), Decision, Version pinning (server ships with Dart SDK; transitively pinned by ADR-0015's Flutter SDK pin; no separate `dart_mcp_server` package to `pub global activate` — the plan draft assumed this and was corrected during execution), Consequences, Rollback, Verification procedure. Created `.mcp.json` and amended `settings.json` (schema validator passes; hooks block untouched). Smoke-tested `dart mcp-server --help` clean (lists real flags `--tools=all|dart`, `--exclude-tool`, `--dart-sdk`, `--flutter-sdk`, `--log-file`). Updated CLAUDE.md in 3 places: bumped Last updated to 2026-05-24; added 2 rows to Executable Commands (`dart mcp-server --help` sanity + `/mcp` listing); extended §"Context7 Mandatory" with a 3-tier precedence rule — Dart MCP first for installed Dart/Flutter packages, Context7 second for not-yet-installed or non-Dart libs, training-data third for stable stdlib only — plus fallback discipline when MCP unavailable. Updated docs/sprints/2026-05-24-m2-ai-harness.md Phase 1 with three executed-time discoveries (Gemini-style format rejected; server is SDK-embedded; smoke test gated by session restart). Updated TODO.md marking Phase 0 ✅ (catch-up) and Phase 1 ✅ with narrative. Two gate-de-aceite items deferred to next session (cannot validate inline in the session that registered the MCP — Claude Code loads MCP servers at session start): `/mcp` shows `dart` ✅ + symbol-resolve smoke test against `MapController` from `flutter_map`. Slice 2 product surface still untouched; sprint branch remains orthogonal at 4 commits ahead.
- [2026-05-24-19 — ai-harness-kickoff](./2026-05-24-19-ai-harness-kickoff.md) — Bootstrapped the M2-AI harness sprint (Phase 0). Research pass on 2026-05-24 (WebSearch + Context7 `/websites/flutter_dev`) covered official Flutter AI tooling (Dart MCP server, AI Rules updated 2026-01-05, Agent Skills doc), community subagents (cleydson/flutter-claude-code, evanca/flutter-ai-rules, VoltAgent/awesome-claude-code-subagents, affaan-m), MCP plugins (Arenukvern/mcp_flutter), and competing spec-driven systems (gmickel/flow-next, gotalab/cc-sdd — both rejected as overlap with our `superpowers:` SDD). Synthesized into `docs/sprints/2026-05-24-m2-ai-harness.md` at repo root: 7-phase playbook with tarefas, gates de aceite binários, riscos, commit messages, and a "how a future session resumes this sprint" section. Created branch `feat/m2-ai-harness` off `feat/m2-slice-2-telas-core` at `7808911`. Authored canonical spec `2026-05-24-ai-harness-upgrade-design.md` with 13 H2 sections and a 6-row Decisions table: Q1 adopt MCP now (highest ROI), Q2 PostToolUse not Stop for codegen hook (sooner regeneration), Q3 mock-lib deferred to Phase 3 inspection, Q4 mcp_flutter deferred to Phase 5 gate, Q5 sprint runs on parallel branch (does not block slice 2), Q6 date-based filename convention not numeric (caught and corrected SPRINT-MD draft drift). Authored companion plan `2026-05-24-ai-harness-upgrade.md` with Phase 0 fully decomposed; Phases 1–7 reference back to SPRINT-MD as canonical playbook. Phase 0 ships zero ADRs by design — ADRs 0021–0026 are phase-scoped. Appended sprint tracking to `TODO.md` (8 checkboxes). Six referenced docs read for convention compliance: CLAUDE.md, TODO.md, both `0000-template.md` files (specs + plans), session template, session index. Validation pipeline planned: in-loop hooks should stay silent on Phase 0 (no Dart edits, no ADR-trigger files), `adr-guardian` not applicable (no stack changes in Phase 0). Slice-2 product surface remains untouched; sprint is fully orthogonal.
- [2026-05-20-18 — ms-01b-statefulshellroute](./2026-05-20-18-ms-01b-statefulshellroute.md) — Closed the Android system-back regression that MS-01 (commit `2124b7f`, push-semantics swap) only partially fixed. Device smoke on Galaxy A06 after MS-01 reproduced the bug: tapping a stop → system back exited the app instead of returning to `/home`. Root-caused via Context7 (`/websites/pub_dev_packages_go_router` changelogs 14.6.1 + 14.8.1) and codewithandrea ("Flutter Bottom Navigation Bar with Stateful Nested Routes using GoRouter"): `/stops/:id` was a flat sibling of `/home`, so the push was semantically a `go`. Filed ADR-0022 documenting the restructure to `StatefulShellRoute.indexedStack` with two `StatefulShellBranch`es (`/home`, `/settings`), per-branch `GlobalKey<NavigatorState>`, pass-through shell builder, and all `/stops/*` + `/optimize/*` routes nested as children of `/home`. Touched 13 caller files updating route literals to the `/home/...` hierarchy. Refactored `HomeBottomNav` to accept `StatefulNavigationShell` and dispatch via `shell.goBranch(index)` with a test-only fallback. Added `integration_test/back_navigation_test.dart` as new hard gate (3 cases: tap stop → back, FAB → AddStop → back, StopDetail → Editar → back) with `_currentScreen` helper inspecting `AppBar.title` text — `GoRouter.of(context)` failed outside the `InheritedGoRouter` scope, and `find.byType(Scaffold).last` was unreliable because `IndexedStack` keeps every branch's Scaffold mounted simultaneously (captured as memory `go-router-stateful-shell-test-context.md`). Added `integration_test: { sdk: flutter }` to dev_dependencies; added the new gate row to `M2-SLICE-CHECKLIST.md`. Three failed PopScope iterations (v1: `context.push` everywhere, v2: shell `PopScope` with dynamic `canPop` via `branchKey.currentState?.canPop()`, v3: `PopScope` always-intercept + runtime `GoRouter.canPop()` decision) all failed to "fix" branch-root back exit — Eduardo manually compared against Gmail/Drive/Photos and confirmed it is standard Android UX, not a bug. Reverted all PopScope code; added ADR-0022 §"Out of scope: branch-root back behavior" citing codewithandrea verbatim. Rewrote memory `popscope-statefulshellroute-android-back.md` (which had been wrong) to document: do not intercept branch-root back; the real bugs were the flat sibling routes, solved by hierarchical nesting. Created memory `subagent-foreground-long-commands.md` after D2 implementer subagent `a9579819f57270127` emitted "Still building. Will wait for notification." and ended its turn without surfacing gate output — rule: subagents running >2min verification commands must run foreground with explicit timeout, never background. Gate verification on Galaxy A06 (USB after Wi-Fi adb dropped during 69 MB APK transfer): `flutter analyze --no-pub` 0 issues, `flutter test` 91/91, integration_test 3/3. Two D4 follow-up edits before commit: stale comment in `app.dart` rewritten, test count mismatch fixed (added the StopDetail → Editar → back case). Two commits landed (`16718e1` architectural fix, `45ffc66` TODO checkpoint) — Lefthook + commitlint passed without `--no-verify`. 2/16 slice-2-fidelity microsprints done; MS-02 widget-home-top-bar is next.
- [2026-05-19-16 — harness-wave-c-mirror-canonical](./2026-05-19-16-harness-wave-c-mirror-canonical.md) — Closed the corrective wave following session-15's QA audit by normalizing the `// Mirror of:` DTO header grammar. Two-stage QA: first general audit of Wave A+B (caught hook fragility, allowlist gap, stack-coverage overstatement), then schema-focused audit (catalogued seven divergent header forms — five docs used Unicode `→`, the canonical reference template `_template.dart` used ASCII `->`, both real DTOs used ASCII `->`, and `auth_dtos.dart` invented a seventh backticked form that silently bypassed the substring-grep hook). Filed ADR-0020 amending ADR-0013 with two normative productions (single-DTO `// Mirror of: <path> -> <Schema>` and multi-DTO `... -> {Schema1, Schema2, ...}`), ASCII `->` only, no backticks, optional parenthesized clarifying notes on per-class `///` markers for composed schemas. Caught and corrected own hallucination during ADR authoring (initially wrote `AuthUserSchema`/`AuthErrorSchema` as TypeBox names; checked the actual `apps/backend/src/auth/schemas.ts` and amended to the real names `UserSchema`/`ErrorResponseSchema` before touching the DTO file). Rewrote `auth_dtos.dart` L1 to the multi-DTO production carrying nine TypeBox schema names verbatim, de-backticked all nine `///` markers, preserved two clarifying notes on `LoginResponseSchema` (intersection) and `RefreshResponseSchema` (alias). Updated `_template.dart` convention block. Swept five docs to ASCII (`CLAUDE.md`, `03-CONVENTIONS.md`, `02-ARCHITECTURE.md`, `M2-SLICE-CHECKLIST.md`, `08-ROADMAP.md`). Hardened `check-dto-mirror.sh` from substring grep to anchored regex (`grep -rlE '^// Mirror of: ${rel_schema} -> '`); verified end-to-end via forged transcript that the hook now correctly identifies `auth_dtos.dart` as orphaned mirror — a silent pass before. Added defensive `tr '\n' '\0' | xargs -0` to `analyze-changed-dart.sh`. Fixed `/verify-slice` `allowed-tools` to include `Agent` (the skill body promised parallel subagent dispatch but the original allowlist forgot the tool). Cross-linked ADR-0020 from ADR-0013, ADR-0018, and the plan template Rule 7. Pre-commit lefthook `mobile-analyze` blocked the second commit because the pre-existing untracked `optimize_route_page.dart` has three info-level issues; resolved with `git stash push --include-untracked` instead of `--no-verify` per CLAUDE.md Git Protocol; restored the stash after Wave C landed. Five Conventional Commits landed (`8fb8c0d`, `f6a6d5f`, `f345ca9`, `f515b9e`, `4841241`). Both real DTOs now compliant with ADR-0020; the hook enforces it mechanically. Harness modernization arc (Waves A + B + C) complete.
- [2026-05-19-15 — harness-wave-b](./2026-05-19-15-harness-wave-b.md) — Closed the harness modernization arc started in session 14 by landing Wave B: extract canonical spec and plan templates from the exemplary slice-2 artifacts, add `/new-spec` and `/new-plan` skills that mechanize the brainstorming-first and writing-plans-first disciplines. Filed ADR-0019 documenting the four options considered (do-nothing, import TLC generic template, extract from slice-2 + add skills, templates-only-no-skills) and the chosen Option C. Authored `docs/superpowers/specs/0000-template.md` preserving slice-2's 13 H2 sections with HTML-comment author guidance under each; authored `docs/superpowers/plans/0000-template.md` preserving the Phase/Task/Step hierarchy + 8 Plan Execution Rules verbatim + Self-Review checklist + Execution Handoff offering subagent-driven vs inline modes. Authored both `.claude/skills/new-spec/SKILL.md` and `.claude/skills/new-plan/SKILL.md` with `disable-model-invocation: true` + `allowed-tools` allowlists scoped to read-only file operations — both skills scaffold the header and STOP at a brainstorming/writing-plans gate, never authoring body content. Wired ADR-0019 into `CLAUDE.md` as a new "Spec-Driven Workflow" subsection. Two self-corrections worth noting: (1) initial ADR-0019 prose claimed "17 H2 sections" four times when the real count is 13; self-audit before any external use caught it, fixed all four occurrences via a follow-up commit (`708902b`) instead of amending — honest history beats clean history; (2) first attempt to commit the templates used scope `superpowers` which is not in `commitlint.config.cjs scope-enum`; lefthook rejected; re-committed as `docs(docs):` matching the precedent for project-meta documentation. Functional smoke-tested both skills' workflows before commit (cp the templates to /tmp, find the slice-2 spec by slug, simulate the no-match path). Five Conventional Commits landed without `--no-verify` (`ac63773`, `708902b`, `9dcd6c5`, `0532736`, `a21fc90`). Wave-A's three Stop-hooks behaved correctly during the session — none fired false positives, confirming the transcript-only signal design decided in session 14. Slice-2 product surface remains untouched from session-13's tip; both harness waves are orthogonal.
- [2026-05-19-14 — harness-wave-a](./2026-05-19-14-harness-wave-a.md) — Modernized the `.claude/` harness with in-loop auto-validation (Wave A only; Wave B deferred). Filed ADR-0018 documenting the four options considered (do-nothing, per-edit blocking, Stop-batch non-blocking, Lefthook-only) and the chosen Stop-batch design that extends ADR-0012's tooling boundary (Claude hooks own intra-turn signal, Lefthook owns commit-boundary enforcement). Authored three Stop-matcher hooks under `.claude/hooks/`: `analyze-changed-dart.sh` runs `flutter analyze --no-pub` over `.dart` files edited this turn under `apps/mobile/lib/`; `check-dto-mirror.sh` mechanizes the ADR-0013 contract by grep'ing `// Mirror of:` headers against edited `apps/backend/src/<feature>/schemas.ts` files; `warn-adr-drift.sh` checks for new/modified ADRs in the working tree when `pubspec.yaml`/`package.json`/`schema.prisma`/`docker-compose.yml` were edited this turn. All three are non-blocking (exit 0 silent, exit 1 warn — `block-env.sh` keeps the exclusive blocking-exit contract) and transcript-only (no working-tree fallback — discovered during smoke-test that `git ls-files --others` caught pre-existing untracked files and would have generated false positives every turn). Authored the `/verify-slice` skill — read-only orchestrator that runs `flutter analyze`, `flutter test`, `bun typecheck`, dispatches `prototype-fidelity-checker` + `adr-guardian` as parallel subagents, and produces one consolidated Markdown report with GO / GO WITH WARNINGS / NO-GO verdict. Wired the three hooks under a new `Stop` matcher in `.claude/settings.json` (timeouts 30/10/10) and updated `CLAUDE.md` with a new "In-Loop Auto-Validation (ADR-0018)" section. Bug found and fixed during validation: initial implementation used `mapfile` (bash-4 builtin) but macOS shipped bash 3.2.57; refactored to `while IFS= read` + `xargs` + `grep -E`. Smoke-tested in six real scenarios before any commit (empty input, non-existent Dart path, real `optimize_route_page.dart` with 3 known infos, pubspec+ADR-present, pubspec+ADR-hidden, schemas.ts+orphan-mirror) — all six matched expected exit codes. Landed three Conventional Commits (`f21b86d`, `b623d48`, `f0bb080`) all passing Lefthook + commitlint without `--no-verify`. Slice-2 work itself untouched; the harness additions are orthogonal.
- [2026-05-19-13 — m2-slice-2-tasks-11-25](./2026-05-19-13-m2-slice-2-tasks-11-25.md) — Executed Tasks 11-25 of the M2 slice-2 plan under `superpowers:subagent-driven-development`: Sub 2a remainder (`StopsController`, `StopListItem`, `ScreenHomeEmpty`, `ScreenHomeList` + GoRouter wiring, `ScreenAddStop` + `StopForm`), Sub 2b foundation + Captura (AndroidManifest permissions + `<queries>` block, `AppPermissions` wrapper, `ScreenVoice`, `ScreenOCR`, `ScreenAddStopsMap`), Sub 2c Manipulação (`ScreenStopDetail` with new `Stop.isGeocoded` getter closing the Null Island contract, `ScreenEditStop` reusing `StopForm` after reconciling the Riverpod 3 `update→updateStop` drift, `ScreenReorder` reusing `StopListItem` in the drag-handle trailing slot, `ScreenMapStops` with OSM tiles per ADR-0016). Three cross-cutting DRY refactors at natural sub-slice boundaries — shared `FakeStopsRepository` consolidating 6 inline fakes (−106 LOC), shared `stopsAsyncView` for `async.when(loading, error, data)` boilerplate across 5 screens (−21 LOC), pre-emptive `FakeAppPermissions` extraction before sub-2b screens spawned the 3rd / 4th copies. Closed the audit gaps the user called out: dispatched `prototype-fidelity-checker` (3 Critical + 4 Important undocumented divergences logged to `TODO.md` as slice-2-PR blockers — HomeEmpty FAB + "Como funciona?" pill, BottomNav across home family, `StopListItem` subtitle semantics, AddStop bottom-sheet, Voice mic visual, OCR dark viewfinder, StopDetail "Parada N de M" title), dispatched `adr-guardian` (zero blocking drift; only follow-up is ADR-0017 owed by Task 26), deleted dead `home_placeholder_page.dart`. Suite 26 → 53 tests; 22 feature commits between `e18f257` and the audit-closure pack. Two memory entries added (Riverpod 3 `update()` reservation, commitlint scope-enum allowlist). Honest accounting in `Open Questions Left` of the session log: I missed several prototype divergences during the per-task implementer dispatches because the prototype reading was scoped per-screen, not to chrome shared across the home family.
- [2026-05-18-12 — m2-slice-2-tasks-1-10](./2026-05-18-12-m2-slice-2-tasks-1-10.md) — Brainstormed slice 2 (locked Q1 persistence = in-memory + `SharedPreferencesAsync`, Q2 default nav = Google Maps + Waze toggle for ADR-0017, Q3 price = BRL 25.90 locked through M2 with re-eval at 50 paying users). Wrote the slice 2 spec (479 lines, 6 self-review gaps patched in-place) and the 42-task implementation plan (6127 lines). Opened/merged PR #7 promoting the roadmap canonical work `develop → main`. Created `feat/m2-slice-2-telas-core` and shipped Tasks 1-10 under `superpowers:subagent-driven-development`: pubspec deps + version bump (1.1.0+2), ADR-0015 amended with resolved versions, `main.dart` edge-to-edge for Android 15, manifest `enableOnBackInvokedCallback` for Android 14, backend `OptimizeResponseSchema` evolved + 200 mock returning input order, `Stop` domain model (5 TDD tests), `StopDto` mirror per ADR-0013 (6 tests), `StopsRepository` interface + `SharedPrefsStopsRepository` impl using `SharedPreferencesAsync` (5 tests), `core/services/id.dart` uuid v4 wrapper (2 tests). 19/19 flutter tests green, `flutter analyze` clean, `bun run typecheck` clean. Every commit landed via Lefthook + commitlint (no `--no-verify`). Three pieces of deferred work: curl smoke for `/routes/optimize` (needs Node 20 + Postgres), real-device E2E (needs Galaxy A06 over adb), `Stop.copyWith` cannot set label to null (resurfaces in Task 22). Branch tip `c6d11f9`, 12 commits ahead of develop.
- [2026-05-13-11 — m2-roadmap-canonical](./2026-05-13-11-m2-roadmap-canonical.md) — Made `docs/08-ROADMAP.md` the single source of truth for M2: rewrote it end-to-end as a per-slice plan (APK ✅ → Telas Core → VRP → Pix → Sentido casa → LGPD → Admin), with library choices validated via Context7 (`flutter_map` 8.x, `speech_to_text` 7.x, `google_mlkit_text_recognition` 0.x, `geolocator` 14.x, `share_plus` 11.x). Created `docs/M2-SLICE-CHECKLIST.md` (rigid per-slice discipline encoding the slice 1 lessons — `aapt2 dump permissions`, `apksigner verify`, real-device E2E with screenshot) and `docs/M2-COST-MODEL.md` (operational target ≤ BRL 200/month total infra in beta, per-transaction unit economics, explicit list of paid services we said no to). Filed ADR-0015 (M2 plan and library choices) and ADR-0016 (map + tile policy: `flutter_map` + public OSM tiles with strict OSMF compliance, documented self-host migration trigger). Propagated the new state across `CLAUDE.md` (Current Focus M1 → M2 / slice 2 next; new onboarding read order), `TODO.md` (slice 1 closed, slice 2 expanded sub-by-sub), `docs/01-PROJECT.md` (M1 ✅, M2 🟡; business model rewritten for pay-per-route), `docs/02-ARCHITECTURE.md` (Flow 3 rewritten for pay-per-route; new Flows 4-6 for map/geocoding/external-nav), `docs/04-FEATURES.md` (status legend, slice mapping per feature, F11 removed), `docs/05-SCREENS.md` (every screen tagged with slice + sub-slice + prototype source), `docs/10-CHANGELOG.md`. Memory entry `m2-source-of-truth-and-read-first-map.md` added so the next session immediately learns the read order and "ROADMAP wins" rule. No code touched; this is a chore PR that resets the canvas for slice 2.
- [2026-05-13-10 — slice-1-validation-and-pr](./2026-05-13-10-slice-1-validation-and-pr.md) — Closed M2 slice 1's validation loop. Walked Eduardo through enabling USB debugging on Android for the first time, installed the v1.0.0 APK on a Samsung Galaxy A06, and hit `Failed host lookup: 'api.roteirizadorpro.com.br'` on the first register attempt. Worked the diagnosis ladder (DNS propagation across 4 resolvers → device-shell ping resolves the host fine → ISP DNS not the culprit → network-security-config not the culprit → Dart-VM negative DNS cache flushed via `am force-stop`) until `aapt2 dump permissions` showed the APK shipping without `android.permission.INTERNET`. Root cause: `flutter create` declares INTERNET only in `src/{debug,profile}/AndroidManifest.xml` overlays, which release builds don't merge. Added the permission to `src/main/AndroidManifest.xml`, rebuilt, re-installed; register → /home → sign-out → re-login round-trip green against production. Captured the gotcha as a follow-up subsection in ADR-0014, in the slice 1 session log, and as a project-scoped feedback memory entry (`flutter-android-release-internet-permission.md`) so a future agent pre-checks `aapt2 dump permissions` before chasing DNS rabbit holes. Opened PR #3 (https://github.com/ZenniTTy/roteirizadorpro.com.br/pull/3) targeting `develop` with the full test plan in the body; Vercel preview deploy reports SUCCESS (the preview URL is gated by Vercel SSO Deployment Protection — expected). Production apex still returns 404 for the APK path pre-merge, also expected. Two manual blockers remain before merge: 1Password backup of the keystore + `.env.deploy` block, and Eduardo's review/merge of PR #3.
- [2026-05-13-09 — m2-slice-1-apk](./2026-05-13-09-m2-slice-1-apk.md) — Opened M2 with the APK-distribution slice. Confirmed M2 sequence with Eduardo (APK → Telas Core → VRP → Pix → Sentido casa → LGPD → Admin). Generated a 30-year PKCS12 keystore RSA-4096 outside the repo, persisted credentials in `.env.deploy` (also captured Efí Bank sandbox + prod credentials shipped mid-session for slice 4), wired `key.properties` + `build.gradle.kts` per the Flutter docs (Context7 `/websites/flutter_dev` consulted), authored `apps/mobile/scripts/build-release-apk.sh`, bumped `pubspec.yaml` to `1.0.0+1` and the manifest label to "Roteirizador Pro". First build with `--split-per-abi` produced clean 16.8 MB/19.3 MB binaries verified by `apksigner` (v2 scheme, cert SHA-256 `D9:C9:61:D6:…:14:31` matching keystore); pivoted to a single 34 MB universal APK to avoid user-facing ABI routing. Published at `apps/landing/public/roteirizador-pro-v1.0.0.apk` with a scoped `!apps/landing/public/*.apk` exception to the global `*.apk` gitignore. Wired the four `Baixar app` CTAs in `apps/landing/src/app/page.tsx` to use the `download` HTML5 attribute. Authored ADR-0014 (Android Release Signing and APK Distribution) covering keystore custody, distribution channel, and build config options. Re-scoped `TODO.md` to M2. Remaining for this slice: physical-device validation (adb install + register/login round-trip), commit + push + PR, and Vercel preview verification. 1Password backup of the keystore is the mandatory manual action before merge.
- [2026-05-09-08 — production-deploy](./2026-05-09-08-production-deploy.md) — Executed Phase 3.B end-to-end on the droplet via DO API + SSH. Provisioned a fresh droplet in NYC3 (DO has no São Paulo datacenter — original ROADMAP assumption was wrong from day one), ran `scripts/server-bootstrap.sh` after cloud-init's runcmd was overridden by DO vendor-data, manual NOPASSWD fix via DO console (server-bootstrap had a bug that skipped the line on re-runs because the user already existed). Switched strategy mid-flight from "rsync graph-cache laptop→droplet" to "build on droplet" because laptop upload was 33-80 KB/s — droplet downloads sudeste from Geofabrik on DO bandwidth + extracts capital SP via `osmium --strategy simple` (smart OOM'd) + builds the graph in ~3 min, with a 2 GB swap file as memory safety net. Bun + Node 20 + bun install + prisma migrate + systemd unit + Nginx + Certbot all green; HTTPS smoke from external network passed; auth round-trip register→login→me verified end-to-end on prod. Production benchmark for criterion #3: cold-start p95 was 205.6 ms (JIT warmup outlier) but three consecutive warm runs landed p95 = 36.5–47.2 ms, ~4-5× under the 200 ms threshold. Backup cron + restore drill verified. DO snapshot `m1-acceptance-baseline-20260509` taken (action 3177273363). M1 acceptance criteria #2 and #3 GREEN; #1 pending landing page; #4 pending client written confirmation.
- [2026-05-08-07 — phase-3-prep](./2026-05-08-07-phase-3-prep.md) — Authored the Phase 3 deploy ammunition without touching the droplet: `scripts/server-bootstrap.sh` (idempotent, codifies the 9-step manual hardening from 07-INFRA), `scripts/backup-postgres.sh` (pg_dump --clean --if-exists + 30d rotation), `scripts/rsync-graph-cache.sh` (laptop→droplet wrapper), `infra/nginx/api.roteirizadorpro.com.br.conf` (pre-Certbot HTTP template), `infra/systemd/roteirizador-backend.service` (Fastify on systemd, hardened), made `infra/docker-compose.yml` env-driven for Postgres credentials, documented production `.env` template in `docs/07-INFRA.md` (the `.env*` hook blocked a `.env.production.example` file and itself suggested 07-INFRA), and wrote `docs/INSTALL.md` — a 10-section runbook from server bootstrap through HTTPS acceptance smoke test. DO API path explored but harness blocked both the API call and the settings edit needed to allow it; manual-SSH path was Eduardo's choice anyway. Phase 3 split into 3.A (prep, done) and 3.B (execute on droplet, next session).
- [2026-05-08-06 — graphhopper-sp](./2026-05-08-06-graphhopper-sp.md) — Stood up GraphHopper locally with capital SP + motorcycle profile + CH, ran the contracted p95 benchmark. Found and worked around four wrong assumptions in ADR-0008 (no `graphhopper/graphhopper` Docker image, no SP-only PBF on any mirror, GraphHopper 8.x removed `vehicle: motorcycle` and made `graph.encoded_values` + `import.osm.ignored_highways` mandatory, build on 1 GB droplet would OOM). New flow: download Sudeste-latest, clip capital SP via `osmium-tool`, build graph on laptop, rsync `graph-cache/` to droplet in Phase 3. Local benchmark: p95 = 64.2 ms (~3.1× under threshold). Amended ADR-0008 in place; added `docs/BENCHMARKS.md`.
- [2026-05-08-05 — mobile-auth-integration](./2026-05-08-05-mobile-auth-integration.md) — Closed the auth slice end-to-end: `flutter_secure_storage` for tokens, Dio + `QueuedInterceptor` with separate `refreshDio` for auto-refresh on 401, `@riverpod AuthController` validating persisted tokens via `/me` on startup, GoRouter redirect reactive to auth state via `ChangeNotifier` bridge, post-login home placeholder, network security config whitelisting cleartext only for the emulator host. Wired Login + Register pages to the controller (with friendly pt-BR error mapping). Smoke-tested on Pixel_8: register-via-curl → login UI → /home renders user payload → Sair → /login. Filed three open observations (mid-session expiry test, cold-start persistence verification, host performance under combined emulator+Docker+IDE load).
- [2026-05-08-04 — backend-auth-healthchecks](./2026-05-08-04-backend-auth-healthchecks.md) — Shipped Phase 2 backend: `/auth/{register,login,refresh,me}` (JWT RS256 + opaque refresh + rotation + OWASP-style reuse detection cascade), `/health{,/db,/graphhopper}`, `/routes/optimize` placeholder. TypeBox schemas as canonical contract per ADR-0013; mobile mirrors authored in same commit set. Standardized backend runtime on `tsx` (Prisma 7 `prisma-client` generator emits TS-only). Smoke-tested 12 paths green. Filed three post-M1 discovery items (argon2id, runtime strategy, refresh-token families).
- [2026-05-08-03 — schema-source-of-truth](./2026-05-08-03-schema-source-of-truth.md) — Codified ADR-0013: Prisma owns the DB, TypeBox owns the HTTP API contract, Dart DTOs mirror TypeBox 1:1 with a `// Mirror of:` header. Added the rule to `CLAUDE.md`, `docs/02-ARCHITECTURE.md` (new "API Contracts & Type Safety" section), `docs/03-CONVENTIONS.md` §8, and an analyzer-clean reference template at `apps/mobile/lib/features/auth/data/dto/_template.dart`. OpenAPI export + Dart codegen explicitly deferred to post-M1; six options weighed in the ADR.
- [2026-05-08-02 — hooks-and-skills-setup](./2026-05-08-02-hooks-and-skills-setup.md) — Implemented the recommendations from `claude-automation-recommender`: Claude Code PreToolUse hook blocking `.env*` edits, PostToolUse hook auto-formatting Dart files in `apps/mobile/`, two read-only subagents (`prototype-fidelity-checker`, `adr-guardian`), two user-only skills (`session-end`, `new-flutter-feature`), and replaced the blanket `.claude/` rule in `.gitignore` with a granular pattern so team-shared automations are committed. Verified Riverpod 3 + go_router patterns via Context7; verified hook semantics via the `claude-code-guide` subagent.
- [2026-05-08-01 — phase-1-foundations](./2026-05-08-01-phase-1-foundations.md) — Executed Phase 1 of the M1 roadmap: backend (Fastify v5 + TypeBox + Prisma 7) skeleton, landing (Next.js 14 + Tailwind), mobile (Flutter source files; flutter create deferred), and `infra/docker-compose.yml` with postgres + redis healthy and graphhopper opt-in via `routing` profile. Adapted `/saas-project-blueprint` to synthesize Blueprint.md from existing ADRs; skipped `/saas-project-scaffold` due to stack collisions.
- [2026-05-07-04 — docs-cleanup](./2026-05-07-04-docs-cleanup.md) — Realigned the docs with the approved Claude Design prototype, focused the roadmap on M1 (deadline 2026-05-26), removed redundant/obsolete files, renumbered to contiguous order, established prototype as canonical UI source.
- [2026-05-05-03 — documentation-backbone](./2026-05-05-03-documentation-backbone.md) — Authored full documentation backbone: foundation files, README, TODO, SECURITY, CODE_OF_CONDUCT, PR template, ten ADRs, and 01-PROJECT / 02-ARCHITECTURE / 04-ROADMAP-M1 / 04-ROADMAP-M2 / 06-DISASTER-RECOVERY docs.
- [2026-05-05-02 — claude-md-rewrite](./2026-05-05-02-claude-md-rewrite.md) — Rewrote CLAUDE.md to comply with Karpathy's four principles and Anthropic's official Claude Code best practices; migrated detail-heavy sections into focused docs.
- [2026-05-05-01 — bootstrap-claude-md](./2026-05-05-01-bootstrap-claude-md.md) — Initial repo bootstrap: stack validation via Context7, CLAUDE.md authored, sessions and decisions structure created.
