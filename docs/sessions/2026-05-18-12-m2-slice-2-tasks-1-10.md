# Session Log — 2026-05-18-12 · M2 Slice 2: Tasks 1-10 (Foundation + DTO layer)

## Metadata

- **Date**: 2026-05-18 (America/Sao_Paulo)
- **Sequence**: 12
- **Agent**: Claude Code (Opus 4.7 [1m])
- **Human**: Eduardo
- **Topic**: M2 slice 2 (Telas Core) — design, plan, and execution of tasks 1-10
- **Duration**: long single session
- **Related ADRs**: ADR-0011, ADR-0013, ADR-0015 (amended), ADR-0016, ADR-0017 (planned for sub 2d)
- **Related TODO items**: M2 slice 2 sub-2a Foundation (in progress)

## Goal of the Session

Onboard into the project's M2 state, run brainstorming to lock the three open slice-2 questions (persistence, default nav, price), write the design spec and the 42-task implementation plan, then start executing the plan under `superpowers:subagent-driven-development` with implementer + spec reviewer + code-quality reviewer per task.

## What Was Done

In order:

- Read the full onboarding sequence (CLAUDE.md → ROADMAP → M2-SLICE-CHECKLIST → M2-COST-MODEL → TODO → sessions index → ADR-0015/0016).
- Confirmed PR #6 merged into `develop` but not promoted to `main`; opened **PR #7** (`develop → main`, docs-only) and Eduardo merged it.
- Created branch `feat/m2-slice-2-telas-core` off `develop` at `4189da3`.
- Ran `superpowers:brainstorming`; locked three decisions for slice 2:
  - **Q1 Persistence**: in-memory Riverpod state + `SharedPreferencesAsync` for session restore; backend `routes` table waits for slice 3.
  - **Q2 Default external nav**: Google Maps default (multi-stop URI), Waze toggle in Settings (parada-por-parada). ADR-0017 will capture during sub 2d.
  - **Q3 Pricing**: BRL 25.90/route locked through M2; revisit only after 50 paying users via data-driven ADR using slice 7 admin metrics.
- Validated new libraries (introduced by the brainstorm) via Context7: `permission_handler`, `uuid` (top-level docs and version pins).
- Wrote spec `docs/superpowers/specs/2026-05-13-m2-slice-2-telas-core-design.md`. Initial commit `24003cb`, amended `c3d1b1e` after self-review surfaced 6 gaps (G1–G6: testing strategy, edge-to-edge, predictive back, `SharedPreferencesAsync`, `flutter_map` 8.x config, accessibility baseline). Force-with-lease pushed.
- After the harness upgrade landed on `develop` mid-session (`49f83ae`), rebased the slice-2 branch onto current `develop`. New post-rebase SHAs: spec `5b586c3`, plan `dffb377`. Force-with-lease pushed.
- Ran `superpowers:writing-plans`. Plan landed at `docs/superpowers/plans/2026-05-13-m2-slice-2-telas-core.md` — 42 tasks, 6127 lines, TDD red→green→commit per task, full code in every step. First commit `5b48056` mistakenly fell on `chore/harness-upgrade`; cherry-picked to the slice-2 branch, reset the harness-upgrade branch back to `49f83ae`.
- Entered `superpowers:subagent-driven-development`. Executed Tasks 1-10 with implementer + spec reviewer + code-quality reviewer per task (some reviews combined where the patch was trivial). Every commit passed lefthook (analyzer + commitlint) without `--no-verify`.

## Decisions Made

1. **PR #6 promotion before slice 2** — Opened PR #7 (`develop → main`, docs-only roadmap canonical work) before branching for slice 2 so `main` reflects the slice-2 ground truth.
2. **Slice 2 = single branch + single PR** — Decided in the plan; sub-slices are commit boundaries, not branch boundaries.
3. **Spec amendments G1-G6** — Six gaps surfaced in critical review of the spec were patched in-place before invoking writing-plans, ensuring the plan was built against a defensible spec.
4. **Backend test strategy = curl smoke, not vitest** — The repo has no backend test framework today. The spec explicitly says install of vitest is post-slice-3 work; slice 2 uses three curl captures in the PR body for 200/401/400 paths.
5. **`shared_preferences_platform_interface` as dev_dep for the SharedPreferencesAsync test substrate** — `InMemorySharedPreferencesAsync` is not re-exported from `package:shared_preferences/shared_preferences.dart`; the official path is to add the platform-interface package as `dev_dependencies` and import the symbols directly.
6. **Transient stub in `routes.ts` during Task 5** — Evolving `OptimizeResponseSchema` (Task 5) made the existing 501 handler's `{ status, message }` literal type-incompatible. Added a 5-line stub returning the new shape on 501 so `tsc --noEmit` stayed green; Task 6 replaced the whole file with the real 200 mock.
7. **Curl smoke for `POST /routes/optimize` deferred** — Step 6.4 needs Node 20 + Postgres + auth token. None available in this session. The capture happens before opening PR feat/m2-slice-2-telas-core → develop.

## Open Questions Left

- [ ] Resolve the Node 20 install on Eduardo's laptop (`nvm install 20`) before Task 6 smoke can run. `.nvmrc` already declares 20.
- [ ] Connect the Galaxy A06 via adb for Task 36-37 real-device E2E.
- [ ] `Stop.copyWith` cannot currently set `label` back to `null` (the `??` semantics treat null as "use current"). Task 22 (ScreenEditStop) is where this surfaces; decide between sentinel pattern, `clearLabel()` method, or empty-string-as-null at the controller layer.
- [ ] Future: instrument the `catch (_)` in `SharedPrefsStopsRepository.load()` once an observability stack lands (post-M2 per `M2-COST-MODEL.md`).

## Files Changed

**Created (slice 2 docs + plan + code):**
- `docs/superpowers/specs/2026-05-13-m2-slice-2-telas-core-design.md`
- `docs/superpowers/plans/2026-05-13-m2-slice-2-telas-core.md`
- `apps/mobile/lib/features/stops/domain/stop.dart`
- `apps/mobile/lib/features/stops/data/dto/stop_dto.dart`
- `apps/mobile/lib/features/stops/data/repositories/stops_repository.dart`
- `apps/mobile/lib/features/stops/data/repositories/shared_prefs_stops_repository.dart`
- `apps/mobile/lib/core/services/id.dart`
- `apps/mobile/test/features/stops/domain/stop_test.dart`
- `apps/mobile/test/features/stops/data/stop_dto_test.dart`
- `apps/mobile/test/features/stops/data/shared_prefs_stops_repository_test.dart`
- `apps/mobile/test/core/services/id_test.dart`

**Modified:**
- `apps/mobile/pubspec.yaml` — 11 deps added, version bumped to `1.1.0+2`; later `dev_dependencies: shared_preferences_platform_interface: ^2.4.2`.
- `apps/mobile/pubspec.lock` — resolver-generated.
- `apps/mobile/lib/main.dart` — `SystemUiMode.edgeToEdge` + transparent system bars.
- `apps/mobile/android/app/src/main/AndroidManifest.xml` — `android:enableOnBackInvokedCallback="true"`.
- `apps/backend/src/routes/schemas.ts` — `StopSchema` exported, `OptimizeResponseSchema` evolved to `{ optimizedOrder, totalDistanceM, totalDurationS }`.
- `apps/backend/src/routes/routes.ts` — replaced 501 placeholder with 200 mock returning input order with zeroed metrics.
- `docs/decisions/0015-m2-plan-and-libraries.md` — table amended with resolved caret-semver pins, dated update note appended.

## Commits Pushed

Branch: `feat/m2-slice-2-telas-core` (origin in sync, last push 2026-05-18).

```
c6d11f9 feat(mobile): add uuid v4 wrapper for local entity ids
4889902 feat(stops): add sharedpreferencesasync-backed stopsrepository
e9207db feat(stops): add stopdto mirror of typebox stopschema
1c3dfea feat(stops): add stop domain model with json roundtrip
7437c1a feat(routes): mock post /routes/optimize returns input order
c45f742 feat(routes): evolve optimize response schema to wire-final shape
9c3a025 feat(mobile): opt into android 14 predictive back animation
cf63313 feat(mobile): adopt android 15 edge-to-edge with transparent system bars
8074aef docs(decisions): record resolved versions in adr-0015
7181fb5 feat(deps): add slice 2 mobile dependencies and bump version to 1.1.0+2
dffb377 docs(stops): m2 slice 2 telas core implementation plan
5b586c3 docs(stops): m2 slice 2 telas core design spec
```

Plus PR #7 (`develop → main`, docs-only roadmap canonical promotion) merged at the start of the session.

## Hand-off Notes for Next Session

**Branch state to resume on:**

```
git checkout feat/m2-slice-2-telas-core
git pull --ff-only origin feat/m2-slice-2-telas-core
git log --oneline origin/develop..HEAD     # 12 commits ahead of develop
flutter --version                          # confirm ≥ 3.24
cd apps/mobile && flutter analyze && flutter test && cd ../..   # 19/19 should pass
```

**Next plan task is 11**: `StopsController` (Riverpod codegen, 7 tests). The plan section starts at line ≈ 1460 of `docs/superpowers/plans/2026-05-13-m2-slice-2-telas-core.md`. Full TDD scaffolding + code is verbatim in the plan; an implementer subagent can execute it without further context once given the task text.

**Plan execution mode**: `superpowers:subagent-driven-development` with implementer + spec reviewer + code-quality reviewer per task. For trivial tasks (≤ ~30 LOC) we combined the two reviewers into one subagent call to save tokens — that pattern worked well for tasks 3, 4, 8, 10.

**Repository conventions to keep honoring:**

- Commitlint forces lowercase identifiers in commit subjects. CamelCase tokens like `StopDto`, `OptimizeResponseSchema`, `TypeBox`, `SharedPreferencesAsync` must be lowercased in the subject line; the body keeps canonical case. Pattern observed in 4 commits this session.
- Lefthook pre-commit runs mobile-analyze on staged `lib/**/*.dart` and backend-typecheck on staged `apps/backend/**/*.ts`. Both must be 0 issues for the commit to land. Never use `--no-verify`.
- Riverpod 3 controllers use `@riverpod class X extends _$X` with `dart run build_runner build --delete-conflicting-outputs`. Commit the generated `.g.dart` companion alongside the source.
- Schema source-of-truth (ADR-0013): TypeBox in `apps/backend/src/<feature>/schemas.ts` evolves first, Dart DTO mirror follows in the same commit when there's a mobile consumer. When the mobile consumer doesn't exist yet (e.g. Task 5 evolved the response shape before Task 27 wires the OptimizeController), the mirror lands in the consumer task.
- DTOs always carry the `// Mirror of: apps/backend/src/<feature>/schemas.ts → <SchemaName>` header.
- `SharedPreferencesAsync` (NOT the deprecated `SharedPreferences.getInstance()`).
- For widget tests using `SharedPreferencesAsync`, add `shared_preferences_platform_interface` as a dev dep and reset `SharedPreferencesAsyncPlatform.instance = InMemorySharedPreferencesAsync.empty();` in `setUp()`.

**Deferred work (must happen before opening the slice 2 PR):**

- Run the three `curl -i` smoke captures for `POST /routes/optimize` (200 / 401 / 400) against a local backend on Node 20 with Postgres up, save into the PR body. See plan Task 6 Step 6.4 for the exact commands.
- Connect the Galaxy A06 for Tasks 36-37 (`aapt2 dump permissions`, `apksigner verify`, 14-step real-device golden path).

**Tech debt items captured for later slices:**

- `Stop.copyWith` cannot set `label` to null. Resolve in Task 22 (ScreenEditStop) — likely via a sentinel pattern or a separate `clearLabel()` method.
- `SharedPrefsStopsRepository.load()` silently swallows corrupt-payload exceptions. Instrument with a non-fatal breadcrumb once Sentry / Crashlytics lands (post-M2 per M2-COST-MODEL).

## Reference Material Used

- Context7: `/websites/pub_dev_permission_handler` (queried during spec authoring; `shouldShowRequestRationale` pattern + "declare in main AndroidManifest" guidance).
- Context7: `/websites/pub_dev_packages_uuid` (RFC 4122 + RFC 9562 compliance, v4 generator).
- Existing ADR-0015 + ADR-0016 (re-read, then ADR-0015 amended in commit `8074aef`).
- `docs/superpowers/specs/2026-05-07-docs-cleanup-design.md` (as a structural template).
- `docs/sessions/2026-05-13-09 / -10 / -11` (recent slice 1 + roadmap canonical history).
